node_modules/
quest-dados.json
quest-dados.json.tmp
quest-dados.json.bak*
*.bak.*
*.log
.DS_Store

# painel de admin local — segredo nunca vai pro git
painel/config.json

# Páginas de referência salvas localmente (HTML do console AWS) — não versionar
paginas AWS/
